version: "3.9"
services:
  web:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8001}
        VITE_GLITCHTIP_DSN: ${VITE_GLITCHTIP_DSN:-}
    ports:
      - "3001:80"
